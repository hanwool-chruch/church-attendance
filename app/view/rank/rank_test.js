'use strict';

describe('myApp.rank module', function() {

	beforeEach(module('myApp.rank'));

	describe('rank controller', function() {

		it('should ....', inject(function($controller) {
			// spec body
			var rankCtrl = $controller('RankCtrl');
			expect(rankCtrl).toBeDefined();
		}));

	});
});